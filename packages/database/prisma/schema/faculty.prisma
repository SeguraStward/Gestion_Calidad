model Faculty {
    id          String   @id @default(auto()) @map("_id") @db.ObjectId
    version     Int      @map("__v") @default(0) // Added version field
    code        String
    description String
    status      String
    schools     School[]

    @@map("faculties")
}
