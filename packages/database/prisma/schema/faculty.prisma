model facultades {
    id          String  @id @default(auto()) @map("_id") @db.ObjectId
    version     Int     @map("__v")@default(0)
    code        String
    description String
    status      String

    @@map("faculties")
}