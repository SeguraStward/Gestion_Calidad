model Faculty {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  version Int    @default(0) @map("__v")

  code        String
  description String
  status      Status

  name String?

  schools School[]

  @@map("faculties")
}
