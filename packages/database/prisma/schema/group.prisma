/// Represents a group.
model Group {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  version   Int                       @map("__v")@default(0)
  status String 
  number String 

  @@map("groups")  
}