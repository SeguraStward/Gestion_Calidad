model IfQuestion {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  version  Int    @default(0) @map("__v")
  status   Status
  question String

  @@map("if_questions")
}

//TODO: I don quite understand the purpose of this model.  
