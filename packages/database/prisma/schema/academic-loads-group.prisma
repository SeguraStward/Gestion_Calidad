model AcademicLoadsGroup {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  version Int    @default(0) @map("__v")

  number String @unique
  status Status

  academicLoads AcademicLoad[]

  @@map("AcademicLoadsGroup")
}
