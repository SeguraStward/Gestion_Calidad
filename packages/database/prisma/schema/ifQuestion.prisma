model IfQuestion {
    id        String @id @default(auto()) @map("_id") @db.ObjectId
    version   Int    @map("__v")@default(0)
    status    String
    question  String

    @@map("if_questions") 
}
