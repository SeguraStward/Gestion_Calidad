model School {
    id          String @id @default(auto()) @map("_id") @db.ObjectId
    version     Int                       @map("__v")@default(0)
    code        String
    description String
    status      String
    idFaculty   String @db.ObjectId
    @@map("schools")
}
